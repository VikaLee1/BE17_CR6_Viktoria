{% extends "base.html.twig" %}
  {% block stylesheets %}
            <link rel="stylesheet" href="{{asset("css/createStyle.css")}}">
        {% endblock %}

{% block body %}


{# {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }} #}

<div class="container createStyle">
<h1 class="text-center m-4 text-white">Create a new event</h1>
<div class="form-control">
{{ form_start(form) }}
    {{ form_widget(form) }}
{{ form_end(form) }}
  <div class="form-error">
        {{ form_errors(form) }}
    </div>
</div>


<a href="{{path('index')}}" class="btn btn-primary mt-4">Go back</a>
</div>


{% endblock %}

